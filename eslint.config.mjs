// Root ESLint flat config. The actual ruleset (incl. frontend import boundaries)
// lives in the shared @su10/eslint-config package so it is reused everywhere.
import su10 from '@su10/eslint-config';

export default su10;
